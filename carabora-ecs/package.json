{
  "name": "carabora-ecs",
  "version": "1.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle src/index.ts --format=esm --minify --sourcemap --outfile=dist/index.js",
    "watch": "esbuild --bundle src/index.ts --format=esm --sourcemap --watch --outfile=dist/index.js",
    "tsc": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.14.5",
    "typescript": "^4.5.4"
  }
}
